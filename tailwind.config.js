export default {
  theme:
    {
      extend: {
        colors: {
          primary: 'var(--primary)',
          gray: 'var(--gray)',
          background: 'var(--background)',
        }
      }
    }
}